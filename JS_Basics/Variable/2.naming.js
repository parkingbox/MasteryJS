// 주석을 달면됨
/**
  *주석이됨
  * 다음줄로 자동으로 넘어감 
 */

  /**변수 규칙!
   * 라틴문자(0-9,a-z, A-Z,), _
   * 대소문자 구분함(단어 두개이상일때)
   * 추천: camelCase (LikeThis)
   * 한국어 X
   * 예약어 X
   * 숫자로 시작 X
   * 특수문자 X(_ , $ 두가지는 예외)
   * 이모지 X 
   * 여러개의 변수를 1, 2, 3 숫자로 구분 X -> 최대한 의미깊게, 구체적인 이름으로 작성!
   */

  let apple;
  let redApple;
  
  // 나쁜예제
  let number = 20;

  // 좋은예제
  let myAge = 20;

  // 나쁜예제
  let audio1;
  let audio2;
  
  // 좋은예제
  let backgroundAudio;
  let windAudio;

  // 꿀팁! 일정한 위치작성
  let audioBackground;
  let audioWind;